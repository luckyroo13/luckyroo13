# Foundry build artifacts
out/
out-solutions/
cache/
broadcast/

# Dependencies (readers install their own if they prefer upstream forge-std)
# NOTE: lib/forge-std IS committed here on purpose — it's a minimal vendored
# subset so the repo is self-contained. Only ignore a fuller install a reader
# adds under a different path.
node_modules/
